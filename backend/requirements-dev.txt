# Dev dependencies (not deployed to Lambda)
-r requirements.txt

# AWS SDK (Lambda provides these, but needed locally)
boto3>=1.34.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Linting/Formatting
black==23.12.1
flake8==7.0.0
